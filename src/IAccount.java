public interface IAccount {
    void deposit(double amount);                     // Corrected method name
    double withdraw(double amount);                  // Corrected method name
    double getCurrentBalance();                      // Corrected method name
    int getAccountNumber();                          // Corrected method name
}
